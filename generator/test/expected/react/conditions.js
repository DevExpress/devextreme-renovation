function testIf(b) { 
    if (b === true) {
        return null;
    }; // TODO - remove comma
    if (b == 1) {
        const a = 10;
        return a;
    } else if (b === 2) { 
        return 0;
    }
}

